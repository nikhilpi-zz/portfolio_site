<section>
  <a href="#" class="image"><img src="{{image_src}}" alt="" data-position="center center" /></a>
  <div class="content">
    <div class="inner">
      <h2>{{title}}</h2>
      <p>{{description}}</p>
      <ul class="actions">
        <li><a href="{{link}}" class="button">Learn more</a></li>
      </ul>
    </div>
  </div>
</section>
